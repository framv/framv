# framv
Render HTML to video or images — directly in the browser. Framv is a fully client-side rendering engine that converts HTML, SVG, and CSS animations into high-resolution videos or still images (up to 4K 120 fps). Ideal for creative tools, video automation, and visual applications — no backend required.

# Function: seekMedia()

> **seekMedia**(`el`, `time`): `Promise`\<`void`\>

Defined in: [media.ts:13](https://github.com/framv/framv/blob/e5a5ad0d58b85c953696f919e1f6065282ef301d/packages/core/src/media.ts#L13)

Seek a media element to `time`, clamped to [0, duration].

## Parameters

### el

`HTMLMediaElement`

### time

`number`

## Returns

`Promise`\<`void`\>

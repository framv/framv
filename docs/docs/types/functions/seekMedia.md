# Function: seekMedia()

> **seekMedia**(`el`, `time`): `Promise`\<`void`\>

Defined in: [media.ts:13](https://github.com/framv/framv/blob/e07368baa204ecae2ca2209c5992b4b5548cebcc/packages/core/src/media.ts#L13)

Seek a media element to `time`, clamped to [0, duration].

## Parameters

### el

`HTMLMediaElement`

### time

`number`

## Returns

`Promise`\<`void`\>
